package com.tianque.plugin.account.validate.impl;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Service;

import com.tianque.core.datatransfer.dataconvert.ValidateHelper;
import com.tianque.core.validate.DomainValidator;
import com.tianque.core.validate.ValidateResult;
import com.tianque.plugin.account.domain.TurnForm;

@Service("turnFormValidatorImpl")
public class TurnFormValidatorImpl implements DomainValidator<TurnForm> {

	@Autowired
	protected ValidateHelper validateHelper;

	@Override
	public ValidateResult validate(TurnForm turnForm) {
		ValidateResult result = new ValidateResult();

		// if (null == turnForm.getId()) {
		// result.addErrorMessage("转办单 ID不能为空");
		// }
		// if (null == turnForm.getStepId()) {
		// result.addErrorMessage("步骤编号不能为空");
		// }
		// if (null == turnForm.getLedgerId()) {
		// result.addErrorMessage("台账编号不能为空");
		// }
		// if (null == turnForm.getLedgerType()) {
		// result.addErrorMessage("台账类型不能为空");
		// }
		// if (null == turnForm.getSourceOrg()) {
		// result.addErrorMessage("当前上报的部门ID不能为空");
		// }
		// if (null == turnForm.getTargetOrg()) {
		// result.addErrorMessage("上级部门ID不能为空");
		// }
		// if (null == turnForm.getReason()) {
		// result.addErrorMessage("转办主要事项及原因不能为空");
		// }
		// if (null == turnForm.getHandleContent()) {
		// result.addErrorMessage("转办主要事项及原因不能为空");
		// }
		// if (null == turnForm.getSerialNumber()) {
		// result.addErrorMessage("转办单编码不能为空");
		// }
		// if (null == turnForm.getName()) {
		// result.addErrorMessage("转办单位联系人不能为空");
		// }
		// if (null == turnForm.getMobile()) {
		// result.addErrorMessage("联系电话不能为空");
		// }
		return result;
	}
}
