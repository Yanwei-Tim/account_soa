package com.tianque.plugin.account.validate.impl;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Service;

import com.tianque.core.datatransfer.dataconvert.ValidateHelper;
import com.tianque.core.validate.DomainValidator;
import com.tianque.core.validate.ValidateResult;
import com.tianque.plugin.account.domain.ReplyForm;

@Service("replyFormValidatorImpl")
public class ReplyFormValidatorImpl implements DomainValidator<ReplyForm> {

	@Autowired
	protected ValidateHelper validateHelper;

	@Override
	public ValidateResult validate(ReplyForm reply) {
		ValidateResult result = new ValidateResult();

		// if (null == reply.getId()) {
		// result.addErrorMessage("回复单 ID不能为空");
		// }
		// if (null == reply.getStepId()) {
		// result.addErrorMessage("步骤编号不能为空");
		// }
		// if (null == reply.getLedgerId()) {
		// result.addErrorMessage("台账编号不能为空");
		// }
		// if (null == reply.getLedgerType()) {
		// result.addErrorMessage("台账类型不能为空");
		// }
		// if (null == reply.getSourceOrg()) {
		// result.addErrorMessage("当前上报的部门ID不能为空");
		// }
		// if (null == reply.getTargetOrg()) {
		// result.addErrorMessage("上级部门ID不能为空");
		// }
		// if (null == reply.getReason()) {
		// result.addErrorMessage("回复主要事项及原因不能为空");
		// }
		// if (null == reply.getHandleContent()) {
		// result.addErrorMessage("回复主要事项及原因不能为空");
		// }
		// if (null == reply.getSerialNumber()) {
		// result.addErrorMessage("回复单编码不能为空");
		// }
		// if (null == reply.getName()) {
		// result.addErrorMessage("回复单位联系人不能为空");
		// }
		// if (null == reply.getMobile()) {
		// result.addErrorMessage("联系电话不能为空");
		// }
		return result;
	}
}
